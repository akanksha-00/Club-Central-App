import 'package:flutter/material.dart';

class ClubRecruitmentsCard extends StatelessWidget {
  const ClubRecruitmentsCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card(
      
    );
  }
}
